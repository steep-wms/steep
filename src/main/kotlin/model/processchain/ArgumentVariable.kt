package model.processchain

/**
 * A variable holding an argument's value
 * @param id the variable's unique identifier
 * @param value the variable's value
 * @author Michel Kraemer
 */
data class ArgumentVariable(val id: String, val value: String)
